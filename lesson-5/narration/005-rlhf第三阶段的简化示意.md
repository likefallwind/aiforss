# RLHF第三阶段的简化示意

这一页展示的是RLHF第三阶段强化学习的简化代码实现。看到代码不用紧张，咱们重点理解背后的逻辑就好。整个流程可以简化成三步：第一步用策略模型生成回复，第二步用奖励模型给这些回复打分，第三步根据分数调整策略模型的参数。具体来说，compute_reward函数接收用户提示和模型生成的回复，编码后送入奖励模型得到一个分数。rlhf_training_step函数则执行单步优化，生成一批回复，计算每个的奖励，然后取负均值作为损失进行梯度更新。代码虽然简化了，但基本思路就是让模型知道什么样的回答得分更高，逐渐调整生成策略朝着人类偏好的方向收敛。