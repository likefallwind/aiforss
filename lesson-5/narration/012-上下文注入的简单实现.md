# 上下文注入的简单实现

这一页展示的是上下文注入法的简化代码实现，逻辑其实很清晰。函数接收三个参数，用户画像、用户问题和模型。首先构建个性化的上下文，把用户背景信息整理成文本格式，包括年龄段、专业领域、交互风格偏好这些字段，如果某个字段缺失就用'未知'填充。然后把用户问题拼在后面，形成完整的输入上下文。最后把上下文编码送入模型生成回复就可以了。代码虽然简单，但已经涵盖了上下文注入法的核心流程，实际项目中可以在这个基础上添加更多用户画像字段或者增加上下文长度的处理逻辑。