# 常见误解

讲完理论咱们来看看几个常见的误解。第一个误解是对齐只是添加安全过滤器。实际上对齐是贯穿模型预训练、指令微调、强化学习整个流程的系统工程，而不是在模型输出之后再简单加一层拦截。第二个误解是认为只要对齐做好了，模型就永远不会输出有害内容。这个想法是不现实的，对齐只能降低有害输出的概率，没办法实现零风险。理解这一点很重要，它帮我们合理设定对AI的期望，也提醒我们在使用时不能完全依赖模型的自我判断。