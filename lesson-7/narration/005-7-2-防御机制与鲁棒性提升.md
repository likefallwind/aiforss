# 7.2 防御机制与鲁棒性提升

现在开始讲防御机制。首先是输入层的防御，也就是在用户输入到达模型之前先过滤一遍。最基本的方法叫提示净化：建立一个敏感指令关键词库，对用户输入进行模式匹配，检测到典型注入模式就告警或拒绝处理。但这种方法有局限性——规则太死板，容易被变体绕过。所以实际应用中需要配合机器学习方法，让检测更智能。这里我给出一个简化版的代码框架，帮助大家理解基本思路。代码里有几个关键点：可疑模式列表、风险评分计算、还有判断是否放行。真正的生产环境会更复杂，比如用神经网络分类器、支持多语言、加入上下文感知等等。这里只是想让大家对输入层防御有个感性认识。