# 安全评测框架

说完了攻击类型，我们来看看怎么评测模型的安全性。学术界和工业界开发了多种评测框架，其中比较有代表性的是HELM框架。HELM从多个维度来评估模型：首先是有害内容检测，看模型面对恶意请求时能不能正确拒绝；其次是鲁棒性测试，评估输入被干扰时模型稳不稳定；第三是指令遵循能力，看模型能不能严格遵守系统指令而不被用户注入带偏；最后还有隐私保护，评估模型会不会泄露训练数据中的敏感信息。这几个维度合在一起，构成了一个相对完整的安全评估体系。这里有个小建议：实际项目中最好结合多个评测框架交叉验证，因为单一评测可能有盲区。如果你想深入了解具体实现，可以去看HELM的官方论文和开源代码。