# 核心概念一：算法偏见的全链路来源

好，刚才我们讲了三条链路，现在做个总结。算法偏见贯穿于机器学习系统的每一个环节。要系统性地理解偏见，必须从数据、模型、目标这三条链路逐一分析。第一，训练数据偏见，它的本质是样本不具代表性或标签本身带有偏见。第二，模型架构偏见，模型的学习机制会放大或固化某些模式。第三，目标函数偏见，默认的优化目标没有考虑公平性约束。理解这三条链路，是后续测量和消解偏见的基础。