# 训练数据偏见

算法偏见并不是什么神秘力量，它就藏在机器学习系统的每一个环节里。我们先把偏见的来源拆成三条链路，第一条是训练数据。训练数据往往是社会现实的映射，而社会现实本身就存在系统性歧视。举个例子，如果用历史招聘数据训练简历筛选模型，过去的录用决定里隐含的性别歧视就会被模型学到，模型会倾向于排斥女性候选人。再比如文本语料库，大量来源于新闻报道，而新闻报道里对某些群体的描述往往跟负面事件关联在一起，语言模型就会学到这些不该学的关联。数据偏见的本质很简单：要么样本不具有代表性，要么标签本身就带着偏见。