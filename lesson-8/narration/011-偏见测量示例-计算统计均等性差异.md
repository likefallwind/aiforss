# 偏见测量示例：计算统计均等性差异

好，现在我们来点实际的，看看怎么用代码计算统计均等性差异。这段Python代码定义了一个函数，计算的是P(Y等于1|A等于1)减去P(Y等于1|A等于0)，结果接近0表示公平，偏离0表示存在偏见。从示例数据来看，y_pred是模型预测，sensitive_attr是敏感属性，最后打印出来的数值如果是正的，就表示模型对受保护群体更倾向于给正预测。这个计算方法简单直接，也是实际审计中经常用到的第一步。