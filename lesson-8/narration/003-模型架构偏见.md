# 模型架构偏见

第二条链路是模型架构。模型的学习机制本身可能会放大或固化某些模式。先说线性模型，它倾向于学习简单的线性关联，但如果某个特征和敏感属性存在伪相关，模型就会把这种关联编码进去。深度神经网络表达能力更强，它特别容易捕捉训练数据里的细微统计规律——问题在于这些规律里可能包含社会偏见的痕迹。举一个图像生成的例子，如果训练集中不同肤色的样本数量差距很大，生成的图像就会出现系统性的人口比例失衡，这不是bug，这就是架构偏见的体现。