# 数据层面消解

先说数据层面的消解。核心策略是重采样和数据增强。重采样的目标是通过过采样少数群体样本或欠采样多数群体样本来平衡训练数据。但要注意，简单的随机重采样可能引入过拟合，尤其在少数群体样本本来就稀缺的情况下。更精细的方法叫SMOTE，合成少数类过采样技术，它通过在少数群体样本之间插值来生成合成样本。数据增强则是通过替换敏感属性的表达方式，比如把he替换为she但保持语义不变，来扩充平衡语料。